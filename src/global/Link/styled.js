import styled from 'styled-components';
import PropTypes from 'prop-types';

const Link = styled.a`
  text-decoration: none;
  :link, :visited {
      text-decoration: none;
  }
`

export default Link;
